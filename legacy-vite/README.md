# Legacy Vite Storefront

This directory preserves the pre-migration Vite/React source for reference. It is not part of the active Next.js build, lint, typecheck, or deployment surface.

The root application is now the production storefront. Do not copy legacy client-side token, routing, or data-fetching patterns into the new App Router code.
